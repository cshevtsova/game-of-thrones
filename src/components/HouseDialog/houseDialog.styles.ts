import { makeStyles } from '@material-ui/core';

export default makeStyles({
  root: {
    margin: 0,
    padding: 10,
    textAlign: 'center',
  },

  closeButton: {
    position: 'absolute',
    right: '1px',
    top: '1px',
    color: 'grey',
  },

});